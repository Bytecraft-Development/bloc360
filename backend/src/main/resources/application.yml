spring:
  application:
    name: backend
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: jdbc:postgresql://localhost:5432/bloc360
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
