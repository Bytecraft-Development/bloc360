server:
  port: 8080
  ssl:
    bundle: "server"
    enabled-protocols: TLSv1.3, TLSv1.2
    enabled: true

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://bloc360.live:8443/realms/bloc360
  ssl:
    bundle:
      jks:
        server:
          truststore:
            location: "classpath:truststore.jks"
            password: ${SSLCERTPASS}
            type: "JKS"
      pem:
        server:
          keystore:
            certificate: "classpath:keystore.p12"
            private-key: "myalias"
            private-key-password: ${SSLCERTPASS}

SSLCERTPASS: ASdcxds32dkjuur
